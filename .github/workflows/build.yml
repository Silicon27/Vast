name: Build main.cpp in cvast

on:
  push:
    paths:
      - cvast/**
  pull_request:
    paths:
      - cvast/**

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y g++ cmake python3-dev

      - name: Configure with CMake
        run: |
          cmake -B build -S .
          cmake --build build
